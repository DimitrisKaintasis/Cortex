# Copy this file to .env for your own reference if useful.
# The CLI reads actual process environment variables; it does not load .env itself.
# Never commit real credentials.

DATA_RETRIEVAL_DB=data.sqlite3
# Prefer a secret manager or shell environment; never commit a real password.
DATA_RETRIEVAL_POSTGRES_DSN=
DATA_RETRIEVAL_POSTGRES_PASSWORD=
# Prefer a secret manager or shell environment; never commit a real password.
DATA_RETRIEVAL_POSTGRES_DSN=
OLLAMA_BASE_URL=http://127.0.0.1:11435
OLLAMA_MODEL=
OLLAMA_EMBEDDING_MODEL=hf.co/mradermacher/harrier-oss-v1-0.6b-GGUF:F16
OLLAMA_EMBEDDING_PROFILE=harrier-retrieval-v1
OLLAMA_TIMEOUT_SECONDS=120
