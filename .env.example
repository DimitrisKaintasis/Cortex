# Copy this file to .env for your own reference if useful.
# The CLI reads actual process environment variables; it does not load .env itself.
# Never commit real credentials.

DATA_RETRIEVAL_DB=data.sqlite3
OLLAMA_BASE_URL=http://127.0.0.1:11435
OLLAMA_MODEL=
OLLAMA_EMBEDDING_MODEL=qwen3-embedding:0.6b
OLLAMA_TIMEOUT_SECONDS=120
